  // 金额验证（正整数或者正小数）
  function isMoney( s ){
      var regu = "^[0-9]+(\\.[0-9]+)?$";
      var re = new RegExp(regu);
      if (re.test(s)) {
      return false;
      } else {
      return true;
      }
  }
